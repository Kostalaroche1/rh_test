# face-api.js models

Place ici les modeles necessaires a la route `/pointage/biometrique`.

Fichiers requis:
- `tiny_face_detector_model-weights_manifest.json` + shards `.bin`
- `face_landmark_68_model-weights_manifest.json` + shards `.bin`
- `face_recognition_model-weights_manifest.json` + shards `.bin`

Chemin charge cote client:
- `/models/face-api`

